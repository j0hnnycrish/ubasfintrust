# Frontend (Vite) Environment Variables Example

# API endpoints
VITE_API_URL=http://localhost:5000/api/v1
VITE_SOCKET_URL=http://localhost:5000

# Payments
# When true, deposits simulate completion via server endpoint (no Stripe needed)
VITE_SIMULATE_PAYMENTS=true
# Optional: if you want to use Stripe in the browser later
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXXXXXXXXXX

# Minimums and UI defaults
# Amounts are in major units (USD)
VITE_MINIMUM_DEPOSIT=1
VITE_MINIMUM_WITHDRAWAL=20

# Optional UI extras
VITE_APP_NAME=UBAS Financial Trust
# Comma-separated hero image URLs
# VITE_HERO_IMAGES=https://example.com/1.jpg,https://example.com/2.jpg
VITE_HERO_ROTATION_MS=7000
VITE_HERO_HEIGHT_VH=50

# Notes:
# 1) Only variables prefixed with VITE_ are exposed to the frontend
# 2) Copy to .env for local dev, set real values in your hosting provider for production
